// Pfriends berjalan sebagai SPA statis: seluruh data mockup hidup di browser
// (Dexie/IndexedDB), sehingga tidak ada yang bisa dirender di server.
export const ssr = false;
export const prerender = false;
